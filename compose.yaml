networks:
  network2:
    name: network2

services:
  nginx:
    container_name: nginx
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./vhost.conf:/etc/nginx/conf.d/default.conf
      - ./www:/app
    networks:
      - network2
    depends_on:
      - php

  php:
    container_name: php
    build: ./images/php
    volumes:
      - ./www:/app
    working_dir: /app
    networks:
      - network2
